pipeline {
    agent any

    stages {
        stage('Stream job') {
            steps {
                echo 'xxxxxxxxxxx'
            }
        }
        stage('Stream job eoore') {
            steps {
                sh('cd /aaaaa')
            }
        }
    }
}
